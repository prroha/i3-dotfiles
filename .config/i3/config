# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:JetBrainsMono Nerd Font 11

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Disabled dex-autostart to prevent XFCE leftover apps from launching
# exec --no-startup-id dex-autostart --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.config/i3/lock.sh

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Compositor
exec --no-startup-id picom --config ~/.config/picom/picom.conf -b

# Notifications
exec --no-startup-id dunst

# Wallpaper
exec --no-startup-id feh --bg-fill --no-fehbg ~/.config/i3/wallpaper.png 2>/dev/null

# Autotiling — splits based on window dimensions (dwindle-like)
exec_always --no-startup-id ~/.local/bin/autotiling

# Set brightness on startup (requires brightnessctl: sudo dnf install brightnessctl)
exec --no-startup-id brightnessctl set 40%

# Shortcuts cheatsheet overlay (set-dpi.sh runs first so conky gets correct DPI)
exec --no-startup-id "~/.config/i3/set-dpi.sh && conky -c ~/.config/conky/i3-shortcuts.conf"

# Dynamic workspace icons
exec_always --no-startup-id ~/.local/bin/i3-workspace-names-daemon -config-path ~/.config/i3/workspace-icons.json -d " " -u -n

# Brightness controls
bindsym XF86MonBrightnessUp exec --no-startup-id "brightnessctl set +5% && polybar-msg action brivol hook 0"
bindsym XF86MonBrightnessDown exec --no-startup-id "brightnessctl set 5%- && polybar-msg action brivol hook 0"

# DPI is set via the conky exec line above (set-dpi.sh runs before conky)

# Volume controls (capped at 100% to prevent speaker damage)
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.config/i3/vol-up.sh
bindsym XF86AudioLowerVolume exec --no-startup-id "pactl set-sink-volume @DEFAULT_SINK@ -10% && polybar-msg action brivol hook 0"
bindsym XF86AudioMute exec --no-startup-id "pactl set-sink-mute @DEFAULT_SINK@ toggle && polybar-msg action brivol hook 0"
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec alacritty

# kill focused window
bindsym $mod+Shift+q kill

# Switch between windows
bindsym $mod+Tab exec --no-startup-id rofi -show window
bindsym $mod+Shift+Tab focus prev

# Scratchpad (hidden floating workspace)
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# Screenshot
bindsym Print exec --no-startup-id flameshot gui

# App launcher (rofi)
bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym $mod+Shift+d exec --no-startup-id rofi -show run

# change focus (vim-style h/j/k/l)
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# cycle through windows (works regardless of layout)
bindsym $mod+n focus next
bindsym $mod+p focus prev

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window (vim-style)
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# manual split (rarely needed with autotiling)
bindsym $mod+b split h
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Workspace 5 is pinned to external monitor dynamically via monitor.sh

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# Power menu (lock, suspend, logout, reboot, shutdown)
bindsym $mod+Shift+e exec --no-startup-id ~/.config/i3/powermenu.sh

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Catppuccin Mocha colors
set $rosewater #f5e0dc
set $flamingo  #f2cdcd
set $pink      #f5c2e7
set $mauve     #cba6f7
set $red       #f38ba8
set $maroon    #eba0ac
set $peach     #fab387
set $green     #a6e3a1
set $teal      #94e2d5
set $sky       #89dceb
set $sapphire  #74c7ec
set $blue      #89b4fa
set $lavender  #b4befe
set $text      #cdd6f4
set $subtext1  #bac2de
set $overlay0  #6c7086
set $surface1  #45475a
set $surface0  #313244
set $base      #1e1e2e
set $mantle    #181825
set $crust     #11111b

# Window border colors:       border    bg        text      indicator child_border
client.focused                 $lavender $base     $text     $lavender $lavender
client.focused_inactive        $overlay0 $base     $subtext1 $overlay0 $overlay0
client.unfocused               $surface1 $base     $overlay0 $surface1 $surface1
client.urgent                  $red      $base     $red      $red      $red

# Window border style
default_border pixel 2
default_floating_border pixel 2
hide_edge_borders smart

# No gaps - clean tiling
gaps inner 0
gaps outer 0

# ─── External monitor ───
exec --no-startup-id ~/.config/i3/monitor.sh right
bindsym $mod+Shift+m exec --no-startup-id ~/.config/i3/monitor.sh right
bindsym $mod+Shift+n exec --no-startup-id ~/.config/i3/monitor.sh off
bindsym $mod+Ctrl+l move workspace to output right
bindsym $mod+Ctrl+h move workspace to output left

# ─── Workspace assignments ───
# Browsers → 1
assign [class="firefox"] → number $ws1
assign [class="Firefox"] → number $ws1
assign [class="Brave-browser"] → number $ws1
assign [class="Google-chrome"] → number $ws1
assign [class="Chromium"] → number $ws1
assign [class="falkon"] → number $ws1
assign [class="Falkon"] → number $ws1

# IDEs → 2
assign [class="code"] → number $ws2
assign [class="Code"] → number $ws2
assign [class="jetbrains-idea"] → number $ws2
assign [class="jetbrains-pycharm"] → number $ws2
assign [class="jetbrains-webstorm"] → number $ws2
assign [class="jetbrains-clion"] → number $ws2
assign [class="jetbrains-goland"] → number $ws2
assign [class="Emacs"] → number $ws2
assign [class="Neovide"] → number $ws2

# Terminals → 3
assign [class="Alacritty"] → number $ws3
assign [class="kitty"] → number $ws3
assign [class="xterm"] → number $ws3

# API / DB tools → 4
assign [class="Postman"] → number $ws4
assign [class="postman"] → number $ws4
assign [class="Insomnia"] → number $ws4
assign [class="DBeaver"] → number $ws4
assign [class="MongoDB Compass"] → number $ws4
assign [class="mongodb-compass"] → number $ws4
assign [class="pgadmin4"] → number $ws4
assign [class="Beekeeper Studio"] → number $ws4

# Messaging → 9
assign [class="Slack"] → number $ws9
assign [class="discord"] → number $ws9
assign [class="Discord"] → number $ws9
assign [class="TelegramDesktop"] → number $ws9
assign [class="Signal"] → number $ws9
assign [class="teams-for-linux"] → number $ws9
assign [class="Microsoft Teams"] → number $ws9
assign [class="Ferdium"] → number $ws9
assign [class="whatsapp-for-linux"] → number $ws9

# ─── Floating rules ───
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="dialog"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="Blueman-manager"] floating enable
for_window [class="nm-connection-editor"] floating enable
for_window [class="Gnome-calculator"] floating enable
for_window [class="gnome-system-monitor"] floating enable
for_window [class="Gnome-system-monitor"] floating enable
for_window [class="flameshot"] floating enable
for_window [class="Galculator"] floating enable
for_window [class="Xarchiver"] floating enable
for_window [title="File Transfer*"] floating enable
for_window [title="Open File"] floating enable
for_window [title="Save As"] floating enable

# Disable default i3bar
bar {
        mode invisible
}

# Launch polybar (replaces i3bar)
exec_always --no-startup-id ~/.config/polybar/launch.sh
